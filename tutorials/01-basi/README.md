# 01 - Basics

In this tutorial you learn:
- `disable` / `able`
- variables `let`, `var`, `const`
- namespaces `l.`, `v.`, `c.`
- output with `console.text.var(...)`
- comments

Open `bases.forge` and run it.
